# Evidence

Native compaction evidence belongs here and is not generated by unit tests.

Each record should include the trigger (`manual` or `auto`), Codex task/turn
identifier when available, trajectory ID, Waymark check/resume JSON, the reloaded
`AGENTS.md` source and SHA-256, and the final result. Do not store secrets or full
file contents.
